/**
 * Feel free to change any of these methods
 * They are only here to give some basic direction.
 */

public interface ISecurity {
    String convertLettersToNumbers(String encryptedText);
    int calculateAmount();
}
